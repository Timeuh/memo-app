export type Memo = {
  id: number,
  title: string,
  content: string
}

export type Theme = 'light'| 'dark';

export type Lang = 'fr' | 'en';

export type Title = {
  fr: string,
  en: string
}

export type MemoTitle = {
  fr: string,
  en: string
}